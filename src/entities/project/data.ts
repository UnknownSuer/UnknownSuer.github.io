/**
 * Проекты портфолио берутся из контент-пака (src/content/content.json),
 * который правится через админку /admin.
 */
export { getProject, getProjects, getPortfolioFolders } from "@/shared/content";
